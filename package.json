{
  "name": "relay-grid-sidecar",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "description": "Bootstrap scaffold for the relay-grid sidecar service.",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "check": "tsc --noEmit -p tsconfig.json",
    "test": "tsx --test src/tests/*.test.ts",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@slack/socket-mode": "^2.0.6",
    "@slack/web-api": "^7.15.0",
    "dotenv": "^16.4.7",
    "pino": "^9.6.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
